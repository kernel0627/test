# 第二问分析总结

## 1. 第二问解决什么问题

第二问解决的是在当前维护触发规律和现有维护效果口径下，对设备未来功能寿命进行预测。这里的寿命是功能寿命，主要依据预测透水率及 rolling365 判据是否触发失效。

本问不做维护策略优化，不新增经济寿命硬终止，也不把维护负担作为主寿命硬终止条件。维护频率、维护负担、是否提前更换等策略问题放到第三问处理。

## 2. 使用了哪些第一问结果

Q2 从第一问读取当前维护触发规律、设备核心指标、季节项、长期衰减项、Hmax 与恢复比例质量评估等结果，并据此生成参数、未来维护日程、未来模拟路径和寿命预测表。

当前预测起点为 2026-04-12，涉及设备 10 台。未来维护日程共 1428 行，未来模拟路径共 62534 行。

| 正式表 | 状态 |
| --- | --- |
| 表01_模型参数表.csv | 存在 |
| 表02_未来维护日程表.csv | 存在 |
| 表03_历史回测指标表.csv | 存在 |
| 表04_未来模拟路径表.csv | 存在 |
| 表05_寿命预测结果表.csv | 存在 |
| 表06_模型对比汇总表.csv | 存在 |
| 表07_长期外推参考表.csv | 存在 |
| 表08_寿命过长诊断表.csv | 存在 |
| 表09_Hmax工程保守敏感性表.csv | 存在 |

## 3. 两个模型是什么

`fixed_gain_baseline` 是基准模型：维护日按固定平台增益修复设备状态，用于提供稳定、可解释的对照结果。

`recovery_ratio_main` 是主模型：维护日按恢复比例机制修复设备状态，优先使用设备级或全局恢复比例；当恢复比例来源不足时，内部可以使用 fixed gain fallback。

两者的核心差异只在维护日恢复机制。第二问本轮不修改衰减机制、Hmax 主情景、维护日程生成规则或寿命终止机制。

## 4. 回测结果

历史回测结果来自 `v2/q_2/tables/表03_历史回测指标表.csv`。回测指标包括 MAE、RMSE、`post_maintenance_7d_MAE` 和 `rolling365_MAE`，分别衡量整体误差、维护后短期误差和 rolling365 判据相关误差。

模型选择主要依据 neutral 主情景下的设备级回测 MAE，并结合恢复比例来源是否可靠判断。

| 模型 | MAE | RMSE | post_maintenance_7d_MAE | rolling365_MAE |
| --- | --- | --- | --- | --- |
| fixed_gain_baseline | 8.761 | 11.091 | 10.221 | 1.210 |
| recovery_ratio_main | 12.646 | 16.906 | 14.307 | 2.162 |

## 5. 寿命预测主结果

第二问正文主结果使用 `v2/q_2/tables/表06_模型对比汇总表.csv`。该表是“第二问主寿命预测结果与模型选择汇总表”，在 neutral 主情景下结合历史回测误差选择推荐模型。

`preferred_model` 规则如下：

- 若 `recovery_ratio_main` 的 MAE 不比 `fixed_gain_baseline` 差超过 5%，且恢复比例来源可靠，则推荐 `recovery_ratio_main`。
- 若 `recovery_ratio_main` 内部使用 fixed gain fallback，但 MAE 不比 baseline 差超过 5%，则推荐 `recovery_ratio_with_fixed_gain_fallback`。
- 若 `recovery_ratio_main` 的 MAE 比 `fixed_gain_baseline` 差超过 5%，则推荐 `fixed_gain_baseline`。

表06中的 `preferred_life_date` 和 `preferred_remaining_years` 是正文引用的主寿命日期和剩余寿命年限。`selection_reason` 与 `note` 必须和 `preferred_model` 保持一致。

| 设备 | 推荐寿命日期 | 推荐剩余年限 | 推荐模型 | 固定增益MAE | 恢复比例MAE | 选择原因 |
| --- | --- | --- | --- | --- | --- | --- |
| a1 | 2029-09-03 | 3.39 | recovery_ratio_main | 10.676 | 3.921 | 恢复比例模型使用 device/global rho，且回测未显著差于固定增益。 |
| a2 | 2027-06-18 | 1.18 | fixed_gain_baseline | 12.102 | 15.551 | 恢复比例模型回测 MAE 超过固定增益 5%，采用固定增益。 |
| a3 | 2027-08-16 | 1.34 | recovery_ratio_main | 9.114 | 7.915 | 恢复比例模型使用 device/global rho，且回测未显著差于固定增益。 |
| a4 | 2031-04-04 | 4.98 | fixed_gain_baseline | 7.464 | 8.589 | 恢复比例模型回测 MAE 超过固定增益 5%，采用固定增益。 |
| a5 | 2028-09-10 | 2.41 | fixed_gain_baseline | 5.601 | 25.141 | 恢复比例模型回测 MAE 超过固定增益 5%，采用固定增益。 |
| a6 | 2028-02-09 | 1.83 | fixed_gain_baseline | 5.986 | 8.996 | 恢复比例模型回测 MAE 超过固定增益 5%，采用固定增益。 |
| a7 | 2028-01-04 | 1.73 | fixed_gain_baseline | 12.211 | 14.724 | 恢复比例模型回测 MAE 超过固定增益 5%，采用固定增益。 |
| a8 | 2027-09-05 | 1.40 | fixed_gain_baseline | 9.748 | 10.906 | 恢复比例模型回测 MAE 超过固定增益 5%，采用固定增益。 |
| a9 | 2028-09-30 | 2.47 | recovery_ratio_main | 3.998 | 3.912 | 恢复比例模型使用 device/global rho，且回测未显著差于固定增益。 |
| a10 | 2027-01-10 | 0.75 | fixed_gain_baseline | 10.708 | 26.801 | 恢复比例模型回测 MAE 超过固定增益 5%，采用固定增益。 |

## 6. 完整情景与敏感性分析

`v2/q_2/tables/表05_寿命预测结果表.csv` 给出了不同模型与 Hmax 情景下的完整寿命预测结果，包括 `fixed_gain_baseline`、`recovery_ratio_main` 以及 optimistic、neutral、pessimistic 等 Hmax 情景。表05是完整情景结果，不把所有行混作第二问正文主寿命结果。

`v2/q_2/tables/表07_长期外推参考表.csv` 只作为超过 30 年预测窗口后的长期外推参考，不参与 `preferred_model` 判断，不作为第二问主结论。

`v2/q_2/tables/表08_寿命过长诊断表.csv` 用于解释某些设备为什么在功能寿命判据下预测较长，辅助判断 Hmax、衰减率、维护频率或 rolling365 判据的影响，不直接改变主寿命结果。

`v2/q_2/tables/表09_Hmax工程保守敏感性表.csv` 只作为 Hmax 更保守下降假设下的敏感性分析，不替代 neutral 主情景，不参与 `preferred_model` 判断。

表05情景覆盖情况如下：

| 模型 | Hmax情景 | 设备数 | 触发寿命终止数 |
| --- | --- | --- | --- |
| fixed_gain_baseline | neutral | 10 | 10 |
| fixed_gain_baseline_hmax_optimistic | optimistic | 10 | 10 |
| fixed_gain_baseline_hmax_pessimistic | pessimistic | 10 | 10 |
| fixed_gain_baseline_major_sensitivity | neutral | 10 | 10 |
| recovery_ratio_main | neutral | 10 | 10 |
| recovery_ratio_main_hmax_optimistic | optimistic | 10 | 10 |
| recovery_ratio_main_hmax_pessimistic | pessimistic | 10 | 10 |
| recovery_ratio_main_major_sensitivity | neutral | 10 | 10 |

## 7. 结果解释

如果某设备在表05中出现 `>30 年` 或 `not_reached_within_30y`，并不表示其精确寿命就是几十年，而是表示在当前功能寿命判据和 30 年预测窗口内没有触发失效。

正文引用时以表06推荐模型结果为主口径。表05、表07、表08、表09分别用于补充展示完整情景、长期外推、原因诊断和 Hmax 敏感性。

## 8. 与第三问的衔接

维护负担、维护频率、提前更换时点和维护策略优化不在第二问中作为硬判寿命条件。第二问只给出当前维护规律下的功能寿命预测；第三问再把维护负担和策略成本收益纳入优化目标。

第二问最终采用表06作为主结果表。表05保留全部模型与 Hmax 情景下的寿命预测结果，表07提供长期外推参考，表08用于解释寿命较长设备的原因，表09用于展示 Hmax 更保守假设下的敏感性。由于第二问目标是当前维护规律下的功能寿命预测，维护负担不作为主寿命硬终止，而作为第三问维护策略优化的重要依据。
